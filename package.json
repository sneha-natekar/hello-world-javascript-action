{
  "name": "hello-world-javascript-action",
  "version": "1.0.0",
  "description": "javascript action repo",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
